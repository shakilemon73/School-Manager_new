# Cloudflare Pages Environment Variables Template
# Copy this file and configure in Cloudflare Dashboard
# Pages → Your Project → Settings → Environment Variables

# ============================================
# SUPABASE CONFIGURATION (REQUIRED)
# ============================================
# Get these from your Supabase project dashboard:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================
# APPLICATION CONFIGURATION
# ============================================
NODE_ENV=production

# ============================================
# OPTIONAL: THIRD-PARTY INTEGRATIONS
# ============================================
# Only add these if you're using these services

# Stripe (Payment Gateway)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_xxxxx

# Anthropic AI (if used)
# VITE_ANTHROPIC_API_KEY=sk-ant-xxxxx

# SSLCommerz (Bangladesh Payment Gateway)
# VITE_SSLCOMMERZ_STORE_ID=your-store-id
# VITE_SSLCOMMERZ_STORE_PASSWORD=your-store-password

# ============================================
# IMPORTANT NOTES
# ============================================
# 
# 1. All environment variables used in the frontend MUST have the VITE_ prefix
#    Example: VITE_SUPABASE_URL (correct) vs SUPABASE_URL (won't work)
#
# 2. Never commit actual values to Git - this is just a template
#
# 3. Set these variables in Cloudflare Dashboard:
#    - Go to Pages → Your Project → Settings → Environment Variables
#    - Add each variable with its value
#    - Set for both "Production" and "Preview" environments
#
# 4. After adding/changing variables, redeploy your application
#
# 5. Sensitive keys (ANON_KEY is public-safe, but SERVICE_KEY is not)
#    should only be used in backend code, not frontend
#
